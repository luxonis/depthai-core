// Generated by https://github.com/foxglove/schemas

syntax = "proto3";

import "daischemas/ArrowPrimitive.proto";
import "daischemas/CubePrimitive.proto";
import "daischemas/CylinderPrimitive.proto";
import "daischemas/KeyValuePair.proto";
import "daischemas/LinePrimitive.proto";
import "daischemas/ModelPrimitive.proto";
import "daischemas/SpherePrimitive.proto";
import "daischemas/TextPrimitive.proto";
import "daischemas/TriangleListPrimitive.proto";
import "google/protobuf/duration.proto";
import "google/protobuf/timestamp.proto";

package dai.schemas;

// A visual element in a 3D scene. An entity may be composed of multiple primitives which all share the same frame of reference.
message SceneEntity {
  // Timestamp of the entity
  google.protobuf.Timestamp timestamp = 1;

  // Frame of reference
  string frame_id = 2;

  // Identifier for the entity. A entity will replace any prior entity on the same topic with the same `id`.
  string id = 3;

  // Length of time (relative to `timestamp`) after which the entity should be automatically removed. Zero value indicates the entity should remain visible until it is replaced or deleted.
  google.protobuf.Duration lifetime = 4;

  // Whether the entity should keep its location in the fixed frame (false) or follow the frame specified in `frame_id` as it moves relative to the fixed frame (true)
  bool frame_locked = 5;

  // Additional user-provided metadata associated with the entity. Keys must be unique.
  repeated dai.schemas.KeyValuePair metadata = 6;

  // Arrow primitives
  repeated dai.schemas.ArrowPrimitive arrows = 7;

  // Cube primitives
  repeated dai.schemas.CubePrimitive cubes = 8;

  // Sphere primitives
  repeated dai.schemas.SpherePrimitive spheres = 9;

  // Cylinder primitives
  repeated dai.schemas.CylinderPrimitive cylinders = 10;

  // Line primitives
  repeated dai.schemas.LinePrimitive lines = 11;

  // Triangle list primitives
  repeated dai.schemas.TriangleListPrimitive triangles = 12;

  // Text primitives
  repeated dai.schemas.TextPrimitive texts = 13;

  // Model primitives
  repeated dai.schemas.ModelPrimitive models = 14;
}
