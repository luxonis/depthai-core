name: DepthAI Core HIL Testing

on:
  workflow_dispatch:
  push:
    branches:
      - v3_develop
    tags:
      - 'v*'
  pull_request:
    branches:
      - v3_develop

# Only allow latest run on same branch to be tested
concurrency:
  group: ci-tests-${{ github.ref }}-1
  cancel-in-progress: true

jobs:

  # Testing
<<<<<<< HEAD
  test:
    env:
      VCPKG_BINARY_SOURCES: "clear;x-gha,readwrite"
=======
  rvc2_test:
>>>>>>> 887ef882e (Adding testbed implementation of CI tests;)
    strategy:
      matrix:
        # os: ['windows', 'macos', 'linux']
        os: ['linux']
        arch: ['x64']
        flavor: ['vanilla', 'asan-ubsan', 'tsan']
        exclude:
          - os: 'windows'
            arch: 'x64'
            flavor: 'tsan'
      fail-fast: false
    runs-on: ['self-hosted', 'testbed-runner']

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: 'recursive'
    - name: Export GitHub Actions cache environment variables
      uses: actions/github-script@v7
      with:
        script: |
          core.exportVariable('ACTIONS_CACHE_URL', process.env.ACTIONS_CACHE_URL || '');
          core.exportVariable('ACTIONS_RUNTIME_TOKEN', process.env.ACTIONS_RUNTIME_TOKEN || '');

    - name: Prepare HIL Framework
      run: source scripts/hil/prepare_hil_framework.sh 

    - name: Configure, Build and Test
      if: matrix.os != 'linux'
      run: |
        export RESERVATION_NAME="https://github.com/$GITHUB_REPOSITORY/actions/$GITHUB_RUN_ID-rvc2-${{ matrix.os }}-${{ matrix.flavor }}"
        CMD="hil --capabilities depthai-core-hil --reservation-name $RESERVATION_NAME -w -s --commands 'cd /tmp/depthai-core|| exit' 'scripts/hil/run_hil_tests.sh ${{ matrix.flavor }} --rvc2'"
        eval $CMD

    - name: Configure, Build and Test
      if: matrix.os == 'linux'
      run: |
<<<<<<< HEAD
        export DISPLAY=:99
        xdpyinfo -display $DISPLAY >/dev/null 2>&1 || (Xvfb $DISPLAY &)
        source venv/bin/activate  # Activate virtual environment
        python3 -m pip install jinja2
        cmake -S . -B build -D CMAKE_BUILD_TYPE=Release -D HUNTER_ROOT=$HOME/.hun2_${{ matrix.flavor }} -D DEPTHAI_BUILD_EXAMPLES=ON -D DEPTHAI_BUILD_TESTS=ON -D DEPTHAI_TEST_EXAMPLES=ON -D DEPTHAI_BUILD_PYTHON=ON -D DEPTHAI_PYTHON_TEST_EXAMPLES=ON -D DEPTHAI_PYTHON_ENABLE_EXAMPLES=ON
        cmake --build build --parallel 4 --config Release
        cd tests
        python3 run_tests.py
=======
        export RESERVATION_NAME="https://github.com/$GITHUB_REPOSITORY/actions/$GITHUB_RUN_ID-rvc2-${{ matrix.os }}-${{ matrix.flavor }}"
        CMD="hil --capabilities depthai-core-hil --reservation-name $RESERVATION_NAME -w -s --commands 'cd /tmp/depthai-core|| exit' 'scripts/hil/run_hil_tests.sh ${{ matrix.flavor }} --rvc2'"
        eval $CMD
      
    # Testing
  rvc4_test:
      strategy:
        matrix:
          # os: ['windows', 'macos', 'linux']
          os: ['linux']
          arch: ['x64']
          flavor: ['vanilla', 'asan-ubsan', 'tsan']
          rvc4os: ['r851.0.23','r851.0.24']
          exclude:
            - os: 'windows'
              arch: 'x64'
              flavor: 'tsan'
        fail-fast: false
      runs-on: ['self-hosted', 'testbed-runner']
  
      steps:
      - uses: actions/checkout@v3
        with:
          submodules: 'recursive'
  
      - name: Prepare HIL Framework
        run: source scripts/hil/prepare_hil_framework.sh 
  
      - name: Configure, Build and Test
        if: matrix.os != 'linux'
        run: |
          export RESERVATION_NAME="https://github.com/$GITHUB_REPOSITORY/actions/$GITHUB_RUN_ID-rvc4-${{ matrix.os }}-${{ matrix.rvc4os }}-${{ matrix.flavor }}""
          CMD="hil --capabilities depthai-core-hil --reservation-name $RESERVATION_NAME -w -s --commands 'cd /tmp/depthai-core|| exit' 'scripts/hil/run_hil_tests.sh ${{ matrix.flavor }} --rvc4 '"
          eval $CMD
  
      - name: Configure, Build and Test
        if: matrix.os == 'linux'
        run: |
          export RESERVATION_NAME="https://github.com/$GITHUB_REPOSITORY/actions/$GITHUB_RUN_ID-rvc4-${{ matrix.os }}-${{ matrix.rvc4os }}-${{ matrix.flavor }}"
          CMD="hil --capabilities depthai-core-hil --reservation-name $RESERVATION_NAME -w -s --rvc4-os-version ${{ matrix.rvc4os }} --commands 'cd /tmp/depthai-core|| exit' 'scripts/hil/run_hil_tests.sh ${{ matrix.flavor }} --rvc4'"
          eval $CMD
>>>>>>> 887ef882e (Adding testbed implementation of CI tests;)
